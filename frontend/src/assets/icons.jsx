// Simple icon components using SVG or emoji placeholders
// You can replace these with actual icon library imports like react-icons

import React from 'react';

export const ChartIcon = ({ className }) => (
  <span className={className}>📊</span>
);

export const BuildingIcon = ({ className }) => (
  <span className={className}>🏢</span>
);

export const SettingsIcon = ({ className }) => (
  <span className={className}>⚙️</span>
);

export const TrendingUpIcon = ({ className }) => (
  <span className={className}>📈</span>
);

export const BoxIcon = ({ className }) => (
  <span className={className}>📦</span>
);

export const CarIcon = ({ className }) => (
  <span className={className}>🚗</span>
);

export const MoneyIcon = ({ className }) => (
  <span className={className}>💰</span>
);

export const LightningIcon = ({ className }) => (
  <span className={className}>⚡</span>
);

export const UsersIcon = ({ className }) => (
  <span className={className}>👥</span>
);

export const UserIcon = ({ className }) => (
  <span className={className}>👤</span>
);

export const ClipboardIcon = ({ className }) => (
  <span className={className}>📋</span>
);

export const WarningIcon = ({ className }) => (
  <span className={className}>⚠️</span>
);

export const RefreshIcon = ({ className }) => (
  <span className={className}>🔄</span>
);

export const RocketIcon = ({ className }) => (
  <span className={className}>🚀</span>
);

export const NotificationIcon = ({ className }) => (
  <span className={className}>🔔</span>
);

export const CheckIcon = ({ className }) => (
  <span className={className}>✅</span>
);

export const WrenchIcon = ({ className }) => (
  <span className={className}>🔧</span>
);
